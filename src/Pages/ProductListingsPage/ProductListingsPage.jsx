import * as React from 'react';
import ProductList from '../../Components/ProductList';

export default class ProductListingsPage extends React.PureComponent {

  render() {
    return (
      <main>
        <ProductList />
      </main>
    );
  }
};